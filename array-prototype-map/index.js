function mapping(arr) {
    const mappedArray = [];
    for ( i = 0; arr.length > 0; i++) {
    arr.map((i)=> { i * 10})
    return mappedArray[i]
    };
    console.log(mappedArray)
    return mappedArray
}
